# Execution reports

Playwright HTML report and test output are generated here after suite runs:

- `html-report/` — HTML report (`npm run report`)
- `test-output/` — traces, screenshots, artifacts

This folder is gitignored for generated content; keep this README for structure.
